import React from 'react'
//import { useSearchParams, useLocation } from 'react-router-dom'

const ReferenceList: React.FC = () => {
    //const [searchParams, setSearchParams] = useSearchParams();
    //const location = useLocation();
    // Get a specific query parameter
    //const myParam = new URLSearchParams(location.search).get('myParam');
    //const myParam = new URLSearchParams(location.search);
    //console.log("my params", myParam)
    //console.log("my params type", myParam.get('type'))
    //console.log("my param count", myParam.get("count"))
  return (
    <div>ReferenceList</div>
  )
}

export default React.memo(ReferenceList)